# Inference_programme — phase 1: programme rewind — quarantine the runs, restart at Phase 1 under batch-and-review

Project: inference_programme
Phase: 1
State: dropped
Gates:
Witness: the mesh/pixelization evidence is out of the active tree: `output/searches/` holds only the point_source and cluster trees, on RAL and on the mirror
Budget: 6:00
Runs: 341908, 341874
Ruling: R-20260831-01
Lane: local-dev
Review-minutes: 5
Epic: jax-inference-profiling
Filed: 2026-08-31
Migrated-from: autolens_profiling/results/notes/inference/DECISIONS.md § 2026-08-31 — Programme rewind (human directive)

## Question

The synthetic phase that carries the 2026-08-31 programme-wide directive. It asks nothing new:
it exists so the REWIND has a phase to be a ruling *of*, and so the two runs the directive
names by id are held in the ledger rather than only in prose.

The directive itself: the inference programme rewinds. The epic ran largely without human
review and every mesh / pixelization result it produced goes to an unphysical solution — the
λ⁴ non-PD overflow flood (341908_5), 59 % of Phase-8B best points clustered at the |e| =
1.41421 box corner of the pixelized cells' box-clipped `ell_comps` geometry, and a
`cond ~ 1e15–1e19` NaN population. Beyond the failures, the human had lost track of what the
programme had actually established.

MGE evidence moved to `output/legacy/` (reusable, pending batch review); mesh / pixelization
evidence moved to `output/legacy_wrong/` (quarantined, never reused or cited except as
failure-mode documentation). All shipped source and registry code stands; only run evidence
and phase/gate state rewind. Gates A, B pt 1 and B pt 2 become **provisional**.

Follow-up filed from the same finding: the `ell_comps` box-corner clustering is
PyAutoGalaxy#594.

## Witness

The quarantine is executed identically on RAL (`/mnt/ral/jnightin/autolens_profiling/output/`)
and on the laptop mirror, directory structure preserved:

- `output/legacy/searches/…` — **MGE**, reusable pending batch review: 83 run-config dirs
  (nautilus 16, nss 13, nuts 2, smc 3, multi_start_prodigy 4, multi_start_prodigy_autoconv 45).
- `output/legacy_wrong/searches/…` — **mesh / pixelization**, unphysical, quarantined: 71 run-config
  dirs (nautilus 21, nss 3, multi_start_prodigy 47), plus RAL-only `slogdet_ab_harvest` and
  `_void_341874_24`.

The active `output/searches/` then holds only the point_source and cluster trees. `hpc/sync pull`
scopes to `output/searches` and passes no `--delete`, so it can never reach into `legacy*/`.

## Where to look

- `autolens_profiling/results/notes/inference/DECISIONS.md` — the 2026-08-31 REWIND entry (the directive of record)
- `autolens_profiling/results/notes/inference/PROGRAMME.md` §"2026-08-31 REWIND" + the annotated phase/gate table
- `inference_programme` (project row): `output/legacy/searches/` and `output/legacy_wrong/searches/`
- autolens_profiling#200 — the queue-of-record anchor for the redo

## Runs

- 341908_5: legacy_wrong — gpu — submitted 2026-08-27 — wall 0:00 — pre-Cortex run, migrated; killed by the likelihood-overflow flood — checkpoint.hdf5 is the only artifact it left
    where: output/legacy_wrong/searches/nautilus/imaging/slam_source_pix_nn/hst/hpc_a100_fp64_ref/c16ff7b02fc168edd8eb9b4aaa4f4bc7
- 341874_24: legacy_wrong — gpu — submitted 2026-08-28 — wall 2:00 — pre-Cortex run, migrated; the RAL-side copy is quarantined as _void_341874_24
    where: output/legacy_wrong/searches/multi_start_prodigy/imaging/knn/hst/n16_s3000_seed4/knn_auto_none_seed4

## Ruling

R-20260831-01 — drop
