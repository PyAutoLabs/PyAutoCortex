# R-20260910-04 — accept analytic_gaussian ensemble_parity

Project: analytic_gaussian
Task: tasks/analytic_gaussian/ensemble_parity.md
Runs: 342413
Ruling: accept
Reviewed-at: 2026-09-10T17:18Z
Review-minutes-actual: 20

## Ruling

ok accept and intake the things to address. we can do another run down the line so also achieve the results in the analytic_gaussian project for future comparison

## Evidence

- `analytic_gaussian` (this project's row): `scripts/ensemble_seed.py` (one seed per array task),
- `autofit_workspace_test` @ `165a19e` — `scripts/graphical/analytic_{reference,ep_minimal,autofit}.py`,
- Mind `draft/research/graphical_ep/ep_campaign.md` — phase 1 and end goal 1
- PyAutoFit#1405 — the collapse basin, still open pending the moment-matching cure
- Mind `draft/bug/graphical_ep/analytic_gaussian_unseeded_graphical_column.md` — the CI script's
- `hpc/batch_cpu/submit_ensemble_n25` — the N=25 rung, written but **not submitted**: its cost is
- Referee: `analytic_gaussian` commit `b44390d` — `results/ens_n5/{summary.json, parity_ensemble.md, parity_ensemble.csv}` + 200 sidecars; job 342413_[0-199], sacct 200/200 COMPLETED exit 0:0, per-seed wall 63–303 s; autofit 2026.8.17.1, dynesty 2.1.4, N = 5, graphical column seeded. Verdict line: **WITNESS NOT MET — 7 met / 4 miss**.
- **Criterion 3 (autofit EP, leg A) MET, 200/200**, worst a = 0.001 against the 0.10 ceiling. The exactly-Gaussian leg is exact as the rule, not the lucky draw — the question the parked CI script raised is closed.
- Criterion 1 (minimal EP leg A) MET: max a = 4.25e-14, max b = 2.22e-16.
- Criterion 5 (autofit graphical): leg A MET 181/200 (0.905 ≥ 0.90); **leg B MISS 175/200 (0.875 vs 0.90)**, no dominant row (192–199/200 each) — edge scatter, not a systematic; leg-B E[sigma] coverage 200/200 [0.981, 1.000].
- **Criterion 4 (autofit EP, leg B) MISS as pre-registered: 78/200 (0.390 vs 0.90)**, entirely the `sigma` row (81/200 cells, a med 0.189 / p90 0.460 / max 2.53; every other row 186–199/200). E[sigma] inside closed-form [q05, q95] 199/200 = 0.995 [0.972, 0.999]; **scale-collapse rate 0/200**. This is the moment-matching cure's baseline number.
- **Criterion 2 (minimal EP, leg B — the analytic ceiling) MISS: 166/200 (0.830 vs 0.95)**, failing row is `mu` (b med 0.053), not sigma (199/200); E[sigma] coverage 200/200. The control misses its own bar → threshold or control is wrong; its own task before the cure is judged against it.
- **Criterion 6 (EP flag health) MISS: 19/400 seed-legs end with a zero-SUCCESS factor** — leg B seeds 29, 38, 44, 52, 62, 71, 75, 83, 90, 106, 133, 138, 147, 149, 152, 160, 178, 193; **leg A seed 140** (the exactly-Gaussian leg). Separate symptom: seeds 25 and 172 `.err` carry `RuntimeWarning: invalid value encountered in add` at `autofit/graphical/expectation_propagation/ep_mean_field.py:336` (NaN in EP evidence accumulation), and neither is in the stale list.
- Accepted with findings: the science answer (means and errors recovered where the graph is Gaussian, biased only where Laplace-on-scatter is known to be) holds; the misses are carried forward as follow-ups filed through /intake (PyAutoFit stale factor on leg A; NaN evidence accumulation) and a Cortex task on criterion 2's threshold. Wave 1 is the baseline for a future rerun.
