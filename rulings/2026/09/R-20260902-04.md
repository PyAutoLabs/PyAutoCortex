# R-20260902-04 — rerun subhalo_validation phase 3

Project: subhalo_validation
Phase: phases/subhalo_validation/delaunay_adapt_split_pl_eff_1_outer.md
Runs: 342027, 342093
Ruling: rerun
Reviewed-at: 2026-09-02T16:40Z

## Ruling

The problem with the pl_eff_1_outer result, for the delaunay, is that the Hilbert mesh is not adapting to the fainter images of the outer source and thus still has too low resolution. I think this is because the adapt_image does not have this scaling, which is used in other scritps, applied to it, which caps the max values to 3.0, meaning fainter sources are up weighted compared to the brightest soures.

For all runs, I can also see like it looks like the adaptive over sampling of the lensed source also is not right with most pixels still 4.

Okay I think we will want to do a rerun of everything for self consistency [...] the rectangular should also used the capped adapt image as even there dont want to over weight bright galaxies, and the aapt images may also impact regularization and thus use this capped image.

Then, we should rerun all subhalo_validation with these updated settings, put old results to a folder titled "incorrect_adapt_sub" and clear them off RAL so tey dont gert in my way.

---

Filed on all three ruled phases with the same body, 2026-09-02: both diagnoses were verified in the run outputs (the uncapped S/N adapt image gave pl_eff_1_outer's outer images 4 Hilbert points each against 79 on the core; the over-sampling call divided the S/N map by the noise map a second time, putting 76-90 % of the mask at sub-size 4 on every lens). Fixed in subhalo_validation commits 9893b12 and 7165a0b; every pre-fix stage output from source_pix[1] onwards and every witness JSON moved to `incorrect_adapt_sub/` on the laptop and on RAL. The accepts R-20260902-02 (pl_eff_0) and R-20260902-03 (rectangular_adapt pl_sersic_0) are superseded because their witnesses were produced under the same two bugs, not because their numbers were wrong. Resubmitted on RAL: delaunay_adapt_split 342231 (job A) → 342234 (job B), rectangular_adapt 342237 → 342240, arrays 0-2.

## Evidence

- `subhalo_validation` (project row): `output/subhalo/detect/delaunay_adapt_split/pl_eff_1_outer_no_subhalo/`
- `output/subhalo/detect/delaunay_adapt_split/pl_eff_1_outer_no_subhalo_adapt_split_fix/source_pix[1]/085f85ba6970436a1b8b115b6c9b3fbf`
- `results/delaunay_adapt_split/pl_eff_1_outer_no_subhalo.json` — the witness
- `results/figures/adapt_split_fix_{regularization,source_plane,residuals}_before_after.jpg`
- `wiki/project/2026-08-31-adapt-split-fix-and-rectangular.md`, `wiki/project/state.md`
