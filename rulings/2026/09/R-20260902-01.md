# R-20260902-01 — accept inference_programme phase 10

Project: inference_programme
Phase: phases/inference_programme/inference_refs_v1_redo.md
Runs: 342091
Ruling: accept
Reviewed-at: 2026-09-02T15:52Z
Follow-ups: autolens_profiling#201

## Ruling

**inference-refs-v1-redo — ACCEPTED, scoped to the four positions-on rows.**

Human, verbatim: *"pos_tauto0.2_f1e8 gives a physical solution which is
acceptable. All folders with this name do, confirming that for a mesh source
this is required to get a physical source and that gradient profiling can be
done robustly. If a fit uses a mesh and does not have a positions.info file,
it should be assumed its result is unreliable and it cannot be used."*

**Binding rule (generalises R-20260901-03):** a mesh / pixelization run dir
without a `positions.info` file is unreliable and cannot be used or cited —
in `output/searches/` or anywhere else. The active tree is to hold no such
run. `pos_tauto0.2_f1e8` is the confirmed physical configuration for mesh
sources; the fixed-threshold `pos_t0.3_f1e8` row (t2) also carries
`positions.info` and is accepted under the same rule.

**Delivery.** All 10 tasks of 342091 delivered on the witness's own terms,
checked from the mirror artefacts: sampler walls 0:35–1:23 (10–23 % of the
6:00 budget); every `.err` is the 222 B mask-padding UserWarning and nothing
else; every `.out` ends "Finished." and every run dir carries `.completed`;
zero "Fit Already Completed"; no overflow signature; the maxLL / logZ table
is bit-identical to `model.results` / `files/samples_info.json` in all ten
run dirs. **One leg is unverified from the laptop:** the result-row leg
(version `2026.8.17.1`, matching `target_id`). The ten rows were written to
RAL `results/searches/nautilus/imaging/…` and have not been pulled or
committed — `origin/main` still carries the 2026-08-26 rows (stale
`priors_ref bf2c8742c334`) and the jump host is throttled. Accepted on the
artefact evidence; the rows are pulled and checked on the next RAL contact
before any is adopted into `InferenceRefs_v1`.

**Rows.**

- **Citable** (`PositionsLH` on, `positions.info` present): t2 `delaunay_nn`
  pos-0.3 (maxLL 31,351.39 / logZ 31,275.23); t4 `slam_source_pix` pos-auto
  (31,547.24 / 31,452.10); t6 `slam_source_pix_nn` pos-auto (31,405.63 /
  31,324.84); t10 `delaunay` pos-auto (31,338.43 / 31,264.83). These four are
  the `InferenceRefs_v1` references for `delaunay_nn_pos_fp64`,
  `slam_source_pix_pos_fp64`, `slam_source_pix_nn_pos_fp64` and
  `delaunay_pos_fp64`.
- **Spent** (positions off, no `positions.info`): t0 `pixelization`, t1
  `delaunay_nn`, t3 `slam_source_pix`, t5 `slam_source_pix_nn`, t7 `knn`,
  t8 `delaunay_matern`. Not citable; their run dirs move to
  `output/legacy_wrong/` on RAL and the mirror. The wave corroborates R-03 on
  its own: in every paired cell the positions-on run sits 520–700 nats
  **above** its positions-off twin (t2−t1 +700.6, t4−t3 +520.6, t6−t5
  +654.6). A penalty cannot raise a likelihood, so the positions-off searches
  ended in a lower basin — the demagnified solution.

**Consequences.**

- The positions-off mesh reference design is retired: SUBMIT_LIST rows
  0/1/3/5/7/8 are struck. `pixelization_fp64`, `knn_fp64` and
  `delaunay_matern_fp64` become positions-on targets and need a fresh wave —
  a new phase, not a rerun of this one. MGE rows are unaffected.
- The stale-knn flag stays CLOSED as a delivery matter, but the "~494 nats
  below the Prodigy arm bar 30,557.03" reading is withdrawn: that bar is a
  Phase 8B arm in `legacy_wrong` and cannot serve as a comparison. knn's
  standing is judged on its positions-on rerun.
- t4 at +25.50 above the truth likelihood is noted, not a finding — a
  pixelized source has more freedom than the truth model.
- Laptop actions (human-directed 2026-09-02): pull and commit the ten result
  rows; `mv` the six positions-off run dirs to `legacy_wrong` (RAL **and**
  mirror), and sweep the active tree for any other mesh run without
  `positions.info`; adopt the four citable rows into
  `results/baselines/InferenceRefs_v1/<key>/` and update
  `INDEX.json`/`INDEX.md`/`SUBMIT_LIST.md`.

## Evidence

- `inference_programme` (project row): `output/searches/nautilus/imaging/` — the 10 run trees
- `logs/output/output.342091_{0-8,10}.out` and the matching `logs/error/` files on the mirror
- `autolens_profiling/results/notes/inference/PROGRAMME.md` — the rewind restart point
- autolens_profiling#200 (queue anchor), #201 (this leg), docs-only PR #202
