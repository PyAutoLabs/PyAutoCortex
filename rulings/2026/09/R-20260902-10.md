# R-20260902-10 — accept inference_programme phase 15

Project: inference_programme
Phase: phases/inference_programme/phase4_positions_gate_b2_reuse.md
Runs: 338525, 340114, 340115, 341892
Ruling: accept
Batch: 2026-09-02-pm
Reviewed-at: 2026-09-02T21:18Z
Follow-ups: autolens_profiling#201

## Ruling

**phase4-positions-gate-b2-reuse — ACCEPTED; GATE B part 2 moves from PROVISIONAL back to CALLED, with a design note for Phase 5.**

Human, 2026-09-02, verbatim: *"continue"* — ratifying the packet recommendation (accept). The
human did not pin Phase 5's penalty factor in this ruling; the note below is the scribe's
recommendation, to be decided when Phase 5 is filed.

**What stands (recomputed from the committed rows):** PositionsLH is safe for gradient MAP on
MGE at factor ≤ 1e5; factor 1e8 is rejected for gradient search. Nautilus is inert on the
posterior — Δ logZ ≤ **0.022 nats** on/off across 5 seeds (t0.3 f1e8), max logL lower with
positions on in 5/5 seeds by a mean 0.126 nats (t = −3.45), wall −3 % to +7 %. Prodigy
n=256 hits: positions-off **5/5**, t0.3 f1e5 **5/5**, t0.3 f1e8 **2/5**, tauto0.2 f1e8 **0/4**
— stiffness, not tightness: a 1e8 slope against a 3×10⁴-scale log-likelihood inflates the
step scale and pins lanes. The six caveats of the 2026-08-27 call ride with it unchanged
(idealised truth positions; one cell / five seeds; 1e5 shown safe, not calibrated; Nautilus
unaffected; no `penalty_at_best` field; `target_id` positions block fixed only in the
2026-08-27 PR). 341892_0 (a resumed run, 68 s) stays excluded as Phase 4 already excluded it.
Stage 1 has no SLURM job — CPU transects, cited by artefact.

**How this sits beside R-20260902-01:** the mesh positions rule and the `pos_tauto0.2_f1e8`
reference rows are **nested-sampling** results — rejection sampling never walks the hinge, and
this phase measures Nautilus inert at f1e8. The "1e8 rejected" finding is about
**fixed-step gradient MAP**. Different engines; no conflict.

**Design note for Phase 5 (recommendation, not yet ruled):** gradient search on a mesh with
positions on should run at factor **1e5** (threshold per the cell's convention), not the
reference rows' f1e8, or the transit damage measured here on MGE returns. Whether MGE's 1e5
transfers to a mesh likelihood scale is unmeasured and is Phase 5's first check.

Laptop action: PROGRAMME.md "Gates A–F" row — GATE B pt 2 CALLED (re-ratified 2026-09-02,
R-20260902-10); the runs stay in `output/legacy/` as cited, reusable evidence.

## Evidence

- `inference_programme` (project row), Stage 2 Nautilus:
- Stage 2 Prodigy:
- Stage 3 Prodigy:
- The positions-off comparator:
- `logs/output/output.<job>_<task>.out` and `logs/error/error.<job>_<task>.err` on the mirror
- `autolens_profiling/results/notes/inference/phase_04_positions/RESULTS.md` (Stages 1, 2, 3)
- Stage 1, which has no run dir: `autolens_profiling/.../transects/transect_{a,b,c}.json` and
- `autolens_profiling/results/notes/inference/DECISIONS.md` § 2026-08-27 (both the W2 Stage-2
- Committed result rows on `main`:
- The rulings that frame the `f1e8` question: R-20260901-01 (340210_9 accepted at
